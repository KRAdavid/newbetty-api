services:
  - type: web
    name: newbetty-api
    env: python
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars:
      - key: KRA_API_KEY
        value: 여기에_당신의_KRA_API_인증키_입력